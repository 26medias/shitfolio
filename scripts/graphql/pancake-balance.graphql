{
  ethereum(network: bsc) {
    address(address: {is: "%wallet%"}) {
      balances {
        currency {
          address
          symbol
          tokenType
        }
        value
      }
    }
  }
}